
# 📘 **Diffrence Of Inheritance, Polymorphism, Abstraction, Encapsulation**

### ✍️ *By Shahid Ahamed*

# OOP Concepts Explained in Bangla

এই README.md ফাইলে Object-Oriented Programming (OOP)-এর চারটি মূল ধারণা — **Inheritance, Polymorphism, Abstraction, Encapsulation** — সহজ বাংলায় ব্যাখ্যা করা হয়েছে।

---

## 🧬 1. Inheritance (ইনহেরিটেন্স — উত্তরাধিকার)

**সংজ্ঞা:** একটি ক্লাস আরেকটি ক্লাসের বৈশিষ্ট্য (properties এবং methods) পেয়ে যায়।

**সহজ উদাহরণ:**
বাবার সম্পদ ছেলেও স্বয়ংক্রিয়ভাবে পায় — এটিই ইনহেরিটেন্স।

**TypeScript উদাহরণ:**

```ts
class Person {
  constructor(public name: string, public age: number) {}
}

class Student extends Person {}

const s1 = new Student("Shahid", 22);
```

---

## 🎭 2. Polymorphism (পলিমরফিজম — বহু রূপ)

**সংজ্ঞা:** একই নামের মেথড, কিন্তু তার কাজ (implementation) ভিন্ন হতে পারে।

**সহজ ভাষায়:** একই কাজ ভিন্নভাবে করা মানেই Polymorphism।

**TypeScript উদাহরণ:**

```ts
class Payment {
  pay(amount: number) {
    console.log(`Paying ${amount} using generic method`);
  }
}

class CreditCardPayment extends Payment {
  pay(amount: number) {
    console.log(`Paying ${amount} using Credit Card`);
  }
}
```

---

## 🕶️ 3. Abstraction (অ্যাবস্ট্র্যাকশন — জটিলতা লুকানো)

**সংজ্ঞা:** জটিল জিনিস লুকিয়ে ইউজারকে শুধু প্রয়োজনীয় ফিচার দেখানো।

**সহজ উদাহরণ:** আপনি ব্রেক চাপলে গাড়ি থামে, কিন্তু ভেতরে হাইড্রোলিক সিস্টেম কিভাবে কাজ করে তা জানার প্রয়োজন নেই।

**TypeScript উদাহরণ:**

```ts
abstract class Vehicle {
  abstract start(): void;
}

class Car extends Vehicle {
  start() {
    console.log("Car Started");
  }
}
```

---

## 🔐 4. Encapsulation (এনক্যাপসুলেশন — তথ্য গোপন/সুরক্ষা করা)

**সংজ্ঞা:** ডেটা এবং মেথডকে একসাথে রেখে অপ্রয়োজনীয় access বন্ধ করে রাখা।

**সহজ উদাহরণ:** ব্যাংকের লকার—ভিতরের সব তথ্য লুকানো থাকে, শুধু নির্দিষ্ট উপায়ে access করা যায়।

**TypeScript উদাহরণ:**

```ts
class BankAccount {
  private balance: number = 0;

  deposit(amount: number) {
    this.balance += amount;
  }

  getBalance() {
    return this.balance;
  }
}
```

---

## 🧲 ৪টি কনসেপ্ট এক লাইনে

| কনসেপ্ট           | ব্যাখ্যা                             |
| ----------------- | ------------------------------------ |
| **Inheritance**   | বাবা-মায়ের সম্পদ সন্তানের কাছে যাওয়া |
| **Polymorphism**  | একই নাম, ভিন্ন কাজ                   |
| **Abstraction**   | জটিলতা লুকিয়ে শুধু দরকারি অংশ দেখানো |
| **Encapsulation** | তথ্য সুরক্ষিত রাখা                   |

---

