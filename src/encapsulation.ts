/*
Encapsulation হলো OOP-এর একটি কনসেপ্ট যেখানে—

ডেটা (properties) এবং

ডেটার উপরে কাজ করা methods

একই ক্লাসের ভিতরে রাখা হয় এবং
বাইরের লোক যাতে ডেটা সরাসরি চেঞ্জ করতে না পারে সেজন্য private, protected ব্যবহার করা হয়।

এতে ডেটা নিরাপদ থাকে এবং ভুলভাবে পরিবর্তন হওয়া থেকে বাঁচে।

🎯 খুব সহজ উদাহরণ (রিয়েল লাইফ)

আপনার ব্যাংক অ্যাকাউন্ট কল্পনা করুন—

আপনার balance কেউ সরাসরি দেখতে বা পরিবর্তন করতে পারে না।

আপনি শুধু ATM / App (যা হচ্ছে methods) এর মাধ্যমে balance দেখতে বা টাকা তুলতে পারবেন।

অর্থাৎ,
ভিতরের বাস্তব ডেটা লুকানো,
আর নির্দিষ্ট দরজার মাধ্যমে অ্যাক্সেস দেওয়া
= Encapsulation

🧪 TypeScript উদাহরণ
*/

class BankAccount {
  private balance: number = 0; // লুকানো তথ্য

  deposit(amount: number) {
    this.balance += amount; // নির্দিষ্ট মেথড দিয়ে ডেটা পরিবর্তন
  }

  getBalance() {
    return this.balance; // নির্দিষ্ট মেথড দিয়ে ডেটা দেখা
  }
}

const acc = new BankAccount();
acc.deposit(1000);
console.log(acc.getBalance()); // 1000

/*👉 বাইরে থেকে balance access করা যাবে না:*/

acc.balance; // ❌ Error (private বলে সরাসরি দেখা যাবে না)

/*🧠 এক লাইনে মনে রাখার টিপস

Encapsulation = ডেটা লুকাও + দরকার মতো access দেও*/

// =========================================================

/*
❌ Without Encapsulation (ডেটা পুরোপুরি উন্মুক্ত / অসুরক্ষিত)

এখানে ব্যালেন্স যে কেউ সরাসরি পরিবর্তন করতে পারছে।
ডেটা সুরক্ষিত নয়।
*/

class BankAccount {
  balance: number = 0; // পাবলিক — সবাই eriş করতে পারে
}

const acc = new BankAccount();

// সরাসরি ডেটা চেঞ্জ!
acc.balance = -5000; 
console.log(acc.balance); // -5000 (ভুল মানেও সেট হয়ে গেল)

// ❗ সমস্যা

// বাইরে থেকে balance ভুলভাবে set হতে পারে

// negative value / ভুল value দিয়ে সিস্টেম নষ্ট হয়ে যেতে পারে

// কোনো security নেই

/*
✅ With Encapsulation (ডেটা সুরক্ষিত + নিয়ন্ত্রিত অ্যাক্সেস)

এখানে balance লুকানো (private)।
পরিবর্তন করা যাবে শুধু নির্দিষ্ট মেথডের মাধ্যমে।
*/

class BankAccount {
  private balance: number = 0; // private = বাইরে থেকে অ্যাক্সেস করা যাবে না

  deposit(amount: number) {
    if (amount > 0) {
      this.balance += amount;
    }
  }

  withdraw(amount: number) {
    if (amount > 0 && amount <= this.balance) {
      this.balance -= amount;
    }
  }

  getBalance() {
    return this.balance;
  }
}

const acc = new BankAccount();

// নির্দিষ্ট মেথড দিয়ে অ্যাক্সেস
acc.deposit(1000);
acc.withdraw(200);

console.log(acc.getBalance()); // 800

/*
✔ সুবিধা

balance কেউ ভুল করে পরিবর্তন করতে পারে না

সিস্টেমের data safe থাকে

validation যোগ করা যায়

হ্যাকার বা ভুল কোড দিয়ে balance=-9999 করা যাবে না
*/

